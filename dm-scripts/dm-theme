#!/usr/bin/env bash
# variables ====================================================================

# functions ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

# setup ________________________________________________________________________
current=$(
    flavours current
)

choice=$(
    flavours list -l |
        wofi --dmenu -p "${current}"
)

[ "$choice" ] || exit 1

# execution ********************************************************************
flavours apply "$choice"
